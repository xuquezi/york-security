# 环境配置
server:
  # 服务端口
  port: 1201
  servlet:
    # 项目contextPath
    context-path: /york
  tomcat:
    # tomcat的URI编码
    uri-encoding: UTF-8
    # tomcat最大线程数，默认为200
    max-threads: 800
    # Tomcat启动初始化的线程数，默认值25
    min-spare-threads: 30

spring:
#redis配置
  redis:
  # Redis服务器地址
    host: 122.51.254.13
  # Redis服务器连接端口
    port: 6379
  # Redis服务器连接密码（默认为空）
    password: 123456
    jedis:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0
        max-wait: -1ms
#activemq配置start
#  activemq:
    #ActiveMQ通讯地址
#    broker-url: tcp://localhost:61616
    #用户名
#    user: admin
    #密码
#    password: admin
    #是否启用内存模式（就是不安装MQ，项目启动时同时启动一个MQ实例）
#    in-memory: false
#    packages:
      #信任所有的包
#      trust-all: true
#    pool:
      #是否替换默认的连接池，使用ActiveMQ的连接池需引入的依赖
#      enabled: false
#activemq配置end
  datasource:
    # 使用阿里的Druid连接池
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver
    # 填写你数据库的url、登录名、密码和数据库名
    url: jdbc:mysql://localhost:3306/york_security?useSSL=false&characterEncoding=utf8
    username: root
    password: pt0ucnc3loHJ
    druid:
      # 连接池的配置信息
      # 初始化大小，最小，最大
      initial-size: 5
      min-idle: 5
      maxActive: 20
      # 配置获取连接等待超时的时间
      maxWait: 60000
      # 配置间隔多久才进行一次检测，检测需要关闭的空闲连接，单位是毫秒
      timeBetweenEvictionRunsMillis: 60000
      # 配置一个连接在池中最小生存的时间，单位是毫秒
      minEvictableIdleTimeMillis: 300000
      validationQuery: SELECT 1 FROM DUAL
      testWhileIdle: true
      testOnBorrow: false
      testOnReturn: false
      # 打开PSCache，并且指定每个连接上PSCache的大小
      poolPreparedStatements: true
      maxPoolPreparedStatementPerConnectionSize: 20
      # 配置监控统计拦截的filters，去掉后监控界面sql无法统计，'wall'用于防火墙
      filters: stat,wall
      # 通过connectProperties属性来打开mergeSql功能；慢SQL记录
      connectionProperties: druid.stat.mergeSql\=true;druid.stat.slowSqlMillis\=5000
      # 配置DruidStatFilter
      web-stat-filter:
        enabled: true
        url-pattern: "/*"
        exclusions: "*.js,*.gif,*.jpg,*.bmp,*.png,*.css,*.ico,/druid/*"
      # 配置DruidStatViewServlet
      stat-view-servlet:
        url-pattern: "/druid/*"
        # IP白名单(没有配置或者为空，则允许所有访问)
        allow: 127.0.0.1
        # IP黑名单 (存在共同时，deny优先于allow)
        # deny: 192.168.1.73
        #  禁用HTML页面上的“Reset All”功能
        reset-enable: false
        # 登录名
        login-username: root
        # 登录密码
        login-password: pt0ucnc3loHJ

mybatis:
  mapper-locations: classpath:mapper/*.xml  #注意：一定要对应mapper映射xml文件的所在路径
logging:
  config: classpath:logback-spring.xml
  # 控制台打印sql，已经在logback-spring.xml中配置相关
  #level:
  # com.example.york.dao: DEBUG

#改用腾讯云的存储对象存储照片
#fdfs配置
#fdfs:
#  so-timeout: 1501
#  connect-timeout: 601
#  thumb-image:             #缩略图生成参数
#    width: 200
#    height: 200
#  tracker-list: 192.168.25.133:22122    #TrackerList参数,支持多个

accessKeyId: LTAI4FwEVizEdcUrdTFL7qqp
accessKeySecret: 7NjQ6Tnwj6MUPtNklzTKtFU1Elak6K
