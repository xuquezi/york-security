<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.york.dao.RoleMapper">
    <resultMap id="MyBaseResultMap" type="com.example.york.entity.Role">
        <id column="role_serial" property="roleId" jdbcType="INTEGER"></id>
        <result column="role_name" property="roleName" jdbcType="VARCHAR"></result>
        <result column="status" property="status" jdbcType="INTEGER"></result>
        <result column="desc" property="desc" jdbcType="VARCHAR"></result>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
        <result column="create_user" property="createUser" jdbcType="VARCHAR" />
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP" />
        <result column="update_user" property="updateUser" jdbcType="VARCHAR" />
        <result column="delete_status" property="deleteStatus" jdbcType="INTEGER"></result>
    </resultMap>

    <select id="getRolesByUserId" resultMap ="MyBaseResultMap">
        SELECT r.* from role r,user_role ur
        where ur.user_id = #{userId}
        and ur.role_id = r.role_serial
        and r.status = 0
        and r.delete_status = 0
    </select>

    <select id="getRoles" resultMap="MyBaseResultMap">
        SELECT r.role_serial,r.role_name
        FROM role r
        WHERE r.delete_status = 0
        and r.status = 0
    </select>
</mapper>